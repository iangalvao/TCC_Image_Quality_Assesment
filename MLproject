# TCC Ian Galvão / IQA

name: image_quality_assesment

docker_env:
  image: iqa_mac0499

entry_points:
  main:
    parameters:
      max_iter: { type: int, default: 100 }
    command: "python src/train.py {max_iter}"
